import socket
print(socket.gethostname())
print(socket.gethostbyname(socket.gethostname())


•GETTING HOST NAME AND LOCAL IP

# Importing socket library
import socket

# Function to display hostname and IP address
def get_Host_name_IP():
    try:
        host_name = socket.gethostname()
        host_ip = socket.gethostbyname(host_name)

        print("Hostname :", host_name)
        print("IP :", host_ip)

    except:
        print("Unable to get Hostname and IP")

# Driver code
get_Host_name_IP()


•GETTING MAC ADDRESS
from getmac import get_mac_address
# Get the MAC address of the default network interface
# It is for your device
mac = get_mac_address()

print(f"MAC Address: {mac}")



from getmac import get_mac_address
mac_remote = get_mac_address (ip="192.168.1.1")
# Example: Router IP
print(f"Remote MAC Address: (mac_remote)")

